part of 'checkout.dart';

class CheckoutDesktop extends StatelessWidget {
  const CheckoutDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Desktop'),
    );
  }
}
